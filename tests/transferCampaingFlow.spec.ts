// import { test } from '../fixtures/fixtures';
// import { createCampaignPayload } from '../payloads/createCampaignPayload';

// test('should succesfully transfer campaign to another user', async ({ panXpanApi, yopmail }) => {
//   const { campaign_id } = await panXpanApi.createCampaign(createCampaignPayload);
//   await panXpanApi.transferCampaign({
//     campaign_id: campaign_id.toString(),
//     transfer_to_email: 'suwezotupru-2442@yopmail.com',
//   });
//   Login with new_owner_email and check if the campaign is present
// });
